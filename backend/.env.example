KALSHI_API_KEY_ID=your-kalshi-api-key-id
KALSHI_PRIVATE_KEY_PATH=./kalshi_private_key.pem
KALSHI_API_BASE=https://api.elections.kalshi.com/trade-api/v2
DASHBOARD_PASSWORD=change-me
SNAPSHOT_INTERVAL_SECONDS=30
DB_PATH=./kalshi_dashboard.db
CORS_ORIGINS=http://localhost:5173
WEATHER_BOT_DB_PATH=
ECON_BOT_DB_PATH=
# Persistent SQLite cache for dashboard's Open-Meteo ensemble responses.
ENSEMBLE_CACHE_DB_PATH=./ensemble_cache.db
# Path to the weather bot's JSON Open-Meteo cache. Read-only — used as a
# secondary lookup before fetching upstream so the dashboard can piggyback
# on whatever the bot has already fetched.
BOT_OM_CACHE_PATH=/home/thumm/Desktop/predict-and-profit-v2/weather-bot/.open_meteo_cache.json
